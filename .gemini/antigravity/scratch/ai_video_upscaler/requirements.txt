# Core dependencies for AI Video Upscaler
# Install with: pip install -r requirements.txt

# Video processing
opencv-python>=4.8.0

# PyTorch (CPU-only build — much smaller download)
# If you have a GPU, remove the --index-url line and just pip install torch torchvision
torch>=2.0.0
torchvision>=0.15.0

# Real-ESRGAN and its dependency (BasicSR)
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
realesrgan>=0.3.0

# Image handling
Pillow>=10.0.0
numpy>=1.24.0

# Progress bar
tqdm>=4.65.0

# Optional: faster video muxing (install separately via package manager)
# ffmpeg — sudo apt install ffmpeg
